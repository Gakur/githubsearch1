import { HeartbeatDirective } from './heartbeat.directive';

describe('HeartbeatDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartbeatDirective();
    expect(directive).toBeTruthy();
  });
});
